{
  "name": "quickpark",
  "version": "1.0.0",
  "description": "QuickPark - Parking management application",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend run dev"
  },
  "dependencies": {
    "@types/pg": "^8.15.6",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
